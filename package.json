{
  "name": "team-task-manager",
  "private": true,
  "version": "1.0.0",
  "description": "Team Task Manager — full-stack hiring assessment build (Express + React + Prisma + Postgres).",
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client ci && npm --prefix client run build && npm --prefix server ci && npm --prefix server run build && npx --prefix server prisma generate",
    "start": "npx --prefix server prisma migrate deploy && node server/dist/index.js",
    "seed": "npm --prefix server run seed",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
